定义插件配置结构。

workflow_templates 字段使用 template_list 类型，包含一个模板：
- enabled: 布尔值，是否启用
- name: 字符串，模板名称
- description: 文本，备注说明
- config_code: 文本，启用代码编辑器，语言为 JSON

debug_mode 字段控制全局调试开关。

提示中说明使用 GitHub Pages 可视化工具生成 JSON 的流程。